using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class ShapeCone : AbstractShape
{
    public override void ApplyShape(SpellScript ss)
    {
        Debug.Log("Cone shape applied");
        //load run animation
        ss.IncRadiusOverTime(1f);
    }

    //runs when shape is added to spell
    public override void AimSpell()
    {
        spellAim.transform.position = GetMouseWorldPos();
    }
    private Vector3 GetMouseWorldPos()
    {
        return aimPos;
    }
}*/
